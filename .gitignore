# Python
__pycache__/
*.pyc

# Node
app/node_modules/
node_modules/
npm-debug.log*

# Capacitor / Android build outputs (native project itself IS committed)
app/android/.gradle/
app/android/build/
app/android/app/build/
app/android/capacitor-cordova-android-plugins/build/
app/android/local.properties
app/android/.idea/
*.iml
.gradle/

# Local toolchain / scratch
test_results/*.log

# OS
.DS_Store

# Design docs / test artifacts — local only (privacy: device inventory, run logs)
PLAN.md
test_results/

# Private workflow — weekly-report drafts, approval markers, Telegram/drafts-repo
# scripts, and project instructions referencing private infra (repo is PUBLIC)
CLAUDE.md
reports/
scripts/

# Build outputs / runtime logs
*.apk
server/server.log
