<!-- ============================================================================
     Outfit Advisor — Phase 2 manifest additions (STAGING SNIPPET, not a full file)

     `npx cap add android` (Phase 3) generates app/android/app/src/main/AndroidManifest.xml.
     Graft the pieces below into that generated manifest. Do NOT ship this file as-is.
     ============================================================================ -->

<!-- (A) Permissions — add these <uses-permission> lines above <application>. -->
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
<uses-permission android:name="android.permission.USE_EXACT_ALARM" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
<uses-permission android:name="android.permission.INTERNET" />
<!-- Deliberately NO ACCESS_BACKGROUND_LOCATION — the FSI visible-activity path
     makes the fire-time read count as foreground location. See PLAN §permission verdict. -->

<!-- (B) On the <application> tag, point it at the cleartext-scoped network config: -->
<!--   android:networkSecurityConfig="@xml/network_security_config" -->
<!--   (Capacitor already sets android:usesCleartextTraffic via cleartext:true; the
        network_security_config narrows cleartext to the DGX IP only.) -->

<!-- (C) Components — add INSIDE <application>…</application>. -->

<!-- The briefly-visible wake screen launched by the FSI notification. -->
<activity
    android:name=".WakeActivity"
    android:exported="false"
    android:excludeFromRecents="true"
    android:noHistory="true"
    android:showWhenLocked="true"
    android:turnScreenOn="true"
    android:theme="@style/Theme.OutfitWake" />

<!-- Daily exact-alarm fire. -->
<receiver
    android:name=".AlarmReceiver"
    android:exported="false">
    <intent-filter>
        <action android:name="com.korety.outfitadvisor.FIRE" />
    </intent-filter>
</receiver>

<!-- Re-arm the alarm after a reboot. -->
<receiver
    android:name=".BootReceiver"
    android:exported="true">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
        <action android:name="android.intent.action.QUICKBOOT_POWERON" />
    </intent-filter>
</receiver>
